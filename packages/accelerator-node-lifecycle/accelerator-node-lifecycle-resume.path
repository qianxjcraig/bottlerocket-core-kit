[Unit]
Description=Watch for durable accelerator lifecycle state changes

[Path]
# state.json is atomically replaced, so watch the stable notification inode
# updated after each durable state save.
PathChanged=/var/lib/accelerator-node-lifecycle/state.json.notify
Unit=accelerator-node-lifecycle-resume.service

[Install]
WantedBy=multi-user.target
