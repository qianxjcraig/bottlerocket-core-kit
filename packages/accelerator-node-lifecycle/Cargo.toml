[package]
name = "accelerator-node-lifecycle"
version = "0.1.0"
edition = "2021"
publish = false
build = "../build.rs"

[package.metadata.build-package]
source-groups = [
    "accelerator-node-lifecycle",
]

[lib]
path = "../packages.rs"

[build-dependencies]
glibc = { path = "../glibc" }
